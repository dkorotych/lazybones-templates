group: deprecated-2017Q3
language: groovy
before_install: "./gradlew buildImages"
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.m2/repository/
    - $HOME/.lazybones/templates/
    - $HOME/.groovy/grapes/
services:
  docker
jdk:
  - oraclejdk7
  - oraclejdk8
script: "./gradlew check testTemplateMavenQuickstart"
